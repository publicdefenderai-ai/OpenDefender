diff --git a/.github/workflows/regression-tests.yml b/.github/workflows/regression-tests.yml
index b63360a..da8c0e0 100644
--- a/.github/workflows/regression-tests.yml
+++ b/.github/workflows/regression-tests.yml
@@ -17,11 +17,17 @@ jobs:
       - name: Set up Node.js
         uses: actions/setup-node@v4
         with:
-          node-version: '20'
+          node-version: '24'
           cache: 'npm'
 
       - name: Install dependencies
-        run: npm ci
+        run: |
+          node --version
+          npm --version
+          npm ci --include=dev --no-audit
+          # Some npm failures have returned success with missing binaries.
+          test -x node_modules/.bin/vitest
+          test -x node_modules/.bin/playwright
 
       - name: Run regression tests
         run: npx vitest run tests/
@@ -39,11 +45,17 @@ jobs:
       - name: Set up Node.js
         uses: actions/setup-node@v4
         with:
-          node-version: '20'
+          node-version: '24'
           cache: 'npm'
 
       - name: Install dependencies
-        run: npm ci
+        run: |
+          node --version
+          npm --version
+          npm ci --include=dev --no-audit
+          # Some npm failures have returned success with missing binaries.
+          test -x node_modules/.bin/vitest
+          test -x node_modules/.bin/playwright
 
       - name: Install Chromium for browser checks
         run: npx playwright install --with-deps chromium
